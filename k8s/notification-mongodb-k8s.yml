apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: notification-mongodb
  namespace: default
spec:
  selector:
    matchLabels:
      app: notification-mongodb
  replicas: 1
  template:
    metadata:
      labels:
        app: notification-mongodb
    spec:
      containers:
      - name: notification-mongodb
        image: microservdemo/piggymetrics-notification-mongodb:{{IMG_VERSION}}
        imagePullPolicy: Always
        ports:
        - containerPort: 27017
        env:
          - name: MONGODB_PASSWORD
            value: "{{MONGODB_PASSWORD}}"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-mongodb
spec:
  ports:
  - name: mongo
    port: 27017
    protocol: TCP
    targetPort: 27017
  selector:
    app: notification-mongodb
  sessionAffinity: None
  type: ClusterIP
